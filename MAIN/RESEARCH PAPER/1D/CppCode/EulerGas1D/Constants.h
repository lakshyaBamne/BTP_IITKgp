#pragma once

#define ll long long int

/*
    Namespace to encapsuate all the named constants used in the program

    -> GAMMA (ratio of specific heats)
    -> THETA (tuning parameter)
    -> etc.
*/

namespace Constants{
    constexpr double GAMMA { 1.4 };
    constexpr double THETA { 1.3 };
}

